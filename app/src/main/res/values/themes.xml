<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StickyNotes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="colorControlActivated">@color/appColor1</item>
        <!-- Change status bar color -->
        <item name="android:statusBarColor">@color/appColor2</item>
        <!-- Make the status bar icons light or dark -->
        <item name="android:windowLightStatusBar">false</item> <!-- For light icons -->
    </style>

    <style name="Theme.StickyNotes" parent="Base.Theme.StickyNotes" />
</resources>